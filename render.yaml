services:
  - type: web
    name: skill-forge-frontend
    env: static
    buildCommand: cd Front-End/Skill-Forge-WebSite && npm install && npm run build
    staticPublishPath: ./Front-End/Skill-Forge-WebSite/build
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
